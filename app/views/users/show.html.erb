<h1><%= @current_user.name %></h1>


<ul>
  <li>email: <%= @current_user.email %></li>
  <li>Phone: <%= @current_user.phone %></li>
</ul>

<div class="controls">
  <%= link_to "Edit details", edit_user_path(@current_user) %>
  <%= link_to "Delete Account", @current, :method => 'delete', :data => { :confirm => 'Are you sure?' } %>
</div>
